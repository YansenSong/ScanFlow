generate_dataset.py
│
├── IR-SIM                 ← 动态障碍 + LiDAR + robot motion

model.py
│
├── LiCS                   ← 2D LiDAR patch embedding
├── DR-SPAAM               ← cross-frame / spatial similarity 思想
└── SCOPE                  ← historical LiDAR + ego-motion 动态建模

train.py
│
├── DR-SPAAM               ← 2D LiDAR training/eval pipeline
└── SCOPE                  ← 时序 prediction training organization

planner.py
│
├── NeuPAN                 ← learning → optimization 架构
└── CasADi / acados        ← NMPC numerical solver